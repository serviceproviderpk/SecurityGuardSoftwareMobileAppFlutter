import 'package:flutter/material.dart';

class visitWebsite extends StatelessWidget {
  const visitWebsite({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Center(
          child: Text("Visit Website"),
        ),
        backgroundColor: Colors.blue,
      ),
    );
  }
}
